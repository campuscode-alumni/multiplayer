<h2><%= t('register.category') %></h2>

<%= simple_form_for @new_category, html: { class: 'col-md-6 mb-4' } do |new| %>
  <%= new.input :name %>
  <%= new.submit t(:submit), class: 'btn btn-primary' %>
<% end %>

<p><%= link_to t(:back), root_path %></p>
